// Tipos Númericos:
// INT (Inteiro): Representa números inteiros, por exemplo, 1, 3, 8.
// FLOAT (Flutuante): Representa números com ponto decimal, por exemplo, 1.38, 3.52, 8.61

// Tipo de Dados:
// STRING: Representa uma sequência de caracteres, envolvida por aspas, por exemplo, "Texto aqui"

// BOOLEANO (BOOL): Representa valores lógicos, pode ser verdadeiro ou falso, usando TRUE ou FALSE

// const = Valor que não pode ser alterado
// let = Valor que pode ser alterado


const result = () => {
    const a = 3;
    const b = 4;
    const result = a + b;

    if (result == 8) {
        console.log(`O resultado é ${true}`);
    } else {
        console.log("O resultado é " + false);
    }
}

result()

const FuncIdade = () => {
    const idade = 18;

    if (idade == 18) {
        console.log("A idade é 18");
    } else {
        console.log("A idade não é 18");
    }
}

FuncIdade()